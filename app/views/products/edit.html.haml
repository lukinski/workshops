%h1 Editing product

%div.row.panel
  %div.col-md-4
    = render 'form'

= link_to 'Show', category_product_path(category, product), class: 'btn btn-success'
= link_to 'Back', category_path(category), class: 'btn btn-primary'
